default:
  suites:
    default:
      paths: [features]
      contexts:
        - Features\Bootstrap\FeatureContext
  autoload:
    - vendor/autoload.php
  formatters:
    progress: ~
    junit:
      output_path: build/behat-junit/junit.xml